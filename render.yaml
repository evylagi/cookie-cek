services:
  - type: web
    name: grok
    runtime: python
    repo: https://github.com/evylagi/cookie-cek
    plan: free
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn grok:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
